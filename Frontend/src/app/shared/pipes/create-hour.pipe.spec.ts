import { CreateHourPipe } from './create-hour.pipe';

describe('CreateHourPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateHourPipe();
    expect(pipe).toBeTruthy();
  });
});
