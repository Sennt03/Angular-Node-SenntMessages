import { CreateUrlPipe } from './create-url.pipe';

describe('CreateUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
